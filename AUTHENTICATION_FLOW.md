# 🔐 Authentication Flow Diagram

## User Journey

```
┌─────────────────────────────────────────────────────────────────┐
│                     COACH FINANCE APP                            │
│                   Authentication Flow                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  User visits    │
│  vipultallekar  │
│     .in         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Authenticated? │◄──────────────┐
└────────┬────────┘               │
         │                        │
    NO   │   YES                  │
         │    │                   │
         ▼    ▼                   │
    ┌─────────────┐          ┌────────────┐
    │   Redirect  │          │  Access    │
    │  to /login  │          │  App       │
    └──────┬──────┘          └────────────┘
           │
           ▼
    ┌──────────────────────────────────┐
    │        LOGIN PAGE                │
    │  ┌────────────────────────────┐  │
    │  │  Sign in with Google       │  │
    │  │  [Google Button]           │  │
    │  └────────────┬───────────────┘  │
    │               │                  │
    │  ┌────────────▼───────────────┐  │
    │  │  OR                        │  │
    │  └────────────┬───────────────┘  │
    │               │                  │
    │  ┌────────────▼───────────────┐  │
    │  │  Email/Password            │  │
    │  │  [Sign In] [Sign Up]       │  │
    │  └────────────┬───────────────┘  │
    └───────────────┼──────────────────┘
                    │
                    ▼
         ┌──────────────────┐
         │  Authentication  │
         │    Successful    │
         └────────┬─────────┘
                  │
                  ▼
         ┌──────────────────┐
         │  Create/Load     │
         │  User Profile    │
         └────────┬─────────┘
                  │
                  ▼
         ┌──────────────────┐
         │  Sync Data from  │
         │    Firestore     │
         └────────┬─────────┘
                  │
                  ▼
         ┌──────────────────┐
         │  Redirect to     │
         │   Home Page      │
         └────────┬─────────┘
                  │
                  ▼
    ┌─────────────────────────────┐
    │     AUTHENTICATED APP       │
    │                             │
    │  ┌───────────────────────┐  │
    │  │  Dashboard            │  │
    │  │  Money Tracker        │  │
    │  │  Insights             │  │
    │  │  Goals                │  │
    │  │  Learn                │  │
    │  │  Profile              │  │
    │  │  Settings             │  │
    │  └───────────────────────┘  │
    │                             │
    │  All data syncs to cloud ☁️ │
    └─────────────────────────────┘
                  │
                  │ User clicks
                  │ "Sign Out"
                  ▼
         ┌──────────────────┐
         │   Sign Out       │
         │   from Firebase  │
         └────────┬─────────┘
                  │
                  └──────────────────┘
                                     │
                         (Loop back to start)
```

## Data Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    DATA SYNCHRONIZATION                       │
└──────────────────────────────────────────────────────────────┘

User Action                 Frontend                  Firestore
─────────────────────────────────────────────────────────────────

Add Transaction  ──────►  useAuth.user.uid  ──────►  users/
                          addTransaction()            {userId}/
                                                      transactions/
                                                      {transId}
                                                         │
                                                         │
                                                         ▼
                                                    ✅ Saved
                                                         │
                                                         │
                          ◄──────────────────────────────┘
                          Real-time
                          Listener
                             │
                             ▼
                        Update UI
                        Show Toast
                        ✅ Success!


Sign In          ──────►  Firebase Auth  ──────────►  OAuth/
on Device 1               signInWithGoogle()          Email Auth
                                │                         │
                                │                         ▼
                                │                    ✅ Verified
                                │                         │
                                ▼                         │
                          Get User ID  ◄─────────────────┘
                                │
                                ▼
                          Load Data from
                          Firestore
                                │
                                ▼
                          users/{userId}/
                          - transactions
                          - budgets
                          - goals
                                │
                                ▼
                          Populate App
                          ✅ Ready!


Sign In          ──────►  Same User ID  ──────────►  Load Same
on Device 2               from Firebase              Data from
                          Auth                       Firestore
                                │                         │
                                ▼                         │
                          Same Data! ◄───────────────────┘
                          ✅ Synced!


Update Budget    ──────►  updateBudget()  ──────►  users/
on Device 1               with user.uid             {userId}/
                                                    budgets/
                                                    {budgetId}
                                                         │
                                                         ▼
                                                    ✅ Updated
                                                         │
                          ◄──────────────────────────────┘
                          Real-time
                          Listener
                             │
Device 2 sees    ◄───────────┘
update instantly!
✅ Auto-synced!
```

## Security Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                            │
└──────────────────────────────────────────────────────────────┘

Request to Access Data
         │
         ▼
┌─────────────────┐
│  Is user        │  NO  ──────►  ❌ Redirect to Login
│  authenticated? │
└────────┬────────┘
         │ YES
         ▼
┌─────────────────┐
│  Get user.uid   │
│  from Firebase  │
│  Auth           │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Firestore      │
│  Security Rules │
│  Check:         │
│  request.auth   │
│  .uid ==        │
│  userId?        │
└────────┬────────┘
         │
    YES  │  NO
         │   │
         │   ▼
         │  ❌ Access Denied
         │     Error: Permission
         │     Denied
         │
         ▼
    ✅ Access Granted
       Return Data


Firestore Security Rules:
─────────────────────────

match /users/{userId} {
  // Only authenticated users
  allow read, write: if request.auth != null 
                     && request.auth.uid == userId;
  
  match /transactions/{transactionId} {
    // Only owner can access
    allow read, write: if request.auth != null 
                       && request.auth.uid == userId;
  }
  
  match /budgets/{budgetId} {
    // Only owner can access
    allow read, write: if request.auth != null 
                       && request.auth.uid == userId;
  }
  
  match /goals/{goalId} {
    // Only owner can access
    allow read, write: if request.auth != null 
                       && request.auth.uid == userId;
  }
}

Result: Each user can ONLY access their own data! 🔒
```

## Component Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    COMPONENT TREE                             │
└──────────────────────────────────────────────────────────────┘

App.tsx
  │
  ├── QueryClientProvider
  │     │
  │     ├── TooltipProvider
  │     │     │
  │     │     ├── AuthProvider ◄──── NEW! Manages auth state
  │     │     │     │
  │     │     │     ├── CurrencyProvider
  │     │     │     │     │
  │     │     │     │     ├── BrowserRouter
  │     │     │     │     │     │
  │     │     │     │     │     ├── Routes
  │     │     │     │     │     │     │
  │     │     │     │     │     │     ├── /login
  │     │     │     │     │     │     │     └── Login.tsx ◄──── NEW!
  │     │     │     │     │     │     │
  │     │     │     │     │     │     ├── / (protected)
  │     │     │     │     │     │     │     └── ProtectedRoute ◄──── NEW!
  │     │     │     │     │     │     │           └── Home.tsx
  │     │     │     │     │     │     │
  │     │     │     │     │     │     ├── /insights (protected)
  │     │     │     │     │     │     │     └── ProtectedRoute
  │     │     │     │     │     │     │           └── Insights.tsx
  │     │     │     │     │     │     │
  │     │     │     │     │     │     └── ... (all other routes)
  │     │     │     │     │     │
  │     │     │     │     │     └── Toasters
  │     │     │     │     │
  │     │     │     │     └── Firebase Context
  │     │     │     │           ├── auth ◄──── NEW!
  │     │     │     │           └── db ◄──── NEW!
  │     │     │     │
  │     │     │     └── Auth State
  │     │     │           ├── user
  │     │     │           ├── loading
  │     │     │           ├── signInWithGoogle()
  │     │     │           ├── signInWithEmail()
  │     │     │           └── signOut()
  │     │     │
  │     │     └── ... (other providers)
  │     │
  │     └── ... (other components)
  │
  └── ... (other wrappers)


Context Providers:
──────────────────

AuthProvider (NEW!)
  │
  ├── Listens to Firebase Auth state
  ├── Provides user object
  ├── Provides auth methods
  └── Manages loading state

CurrencyProvider (Existing)
  │
  ├── Manages currency selection
  └── Provides formatting functions

ProtectedRoute (NEW!)
  │
  ├── Checks if user is authenticated
  ├── Shows loader while checking
  ├── Redirects to /login if not authenticated
  └── Renders children if authenticated
```

## File Structure

```
Coach/
├── webapp/
│   ├── src/
│   │   ├── lib/
│   │   │   ├── firebase.ts ◄──────────── NEW! Firebase config
│   │   │   └── firestore.ts ◄─────────── NEW! DB operations
│   │   │
│   │   ├── contexts/
│   │   │   ├── AuthContext.tsx ◄──────── NEW! Auth provider
│   │   │   └── CurrencyContext.tsx
│   │   │
│   │   ├── components/
│   │   │   ├── auth/
│   │   │   │   └── ProtectedRoute.tsx ◄─ NEW! Route guard
│   │   │   └── ... (other components)
│   │   │
│   │   ├── pages/
│   │   │   ├── Login.tsx ◄────────────── NEW! Login page
│   │   │   ├── Home.tsx
│   │   │   ├── Settings.tsx (modified)
│   │   │   └── ... (other pages)
│   │   │
│   │   └── App.tsx (modified)
│   │
│   └── package.json (firebase added)
│
├── firebase.json ◄────────────────────── NEW! Firebase config
├── .firebaserc ◄──────────────────────── NEW! Project link
├── firestore.rules ◄──────────────────── NEW! Security rules
├── AUTHENTICATION_GUIDE.md ◄──────────── NEW! Dev docs
├── QUICK_START.md ◄───────────────────── NEW! User guide
└── IMPLEMENTATION_SUMMARY.md ◄─────────── NEW! Summary
```

---

**Visual Guide Created**: February 14, 2026
**Status**: Complete with Authentication Flow
